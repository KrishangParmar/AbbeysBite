// Base configuration for the iosApp target.
// Secrets.xcconfig (gitignored) is included when present — copy
// Secrets.example.xcconfig to create it.
#include? "Secrets.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = com.abbeysbite.app
MARKETING_VERSION = 1.0.0
CURRENT_PROJECT_VERSION = 1
IPHONEOS_DEPLOYMENT_TARGET = 15.6
